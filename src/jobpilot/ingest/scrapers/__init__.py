"""One scraper module per applicant tracking system (ATS), all sharing
the `BaseScraper` contract in `base.py`. Scraping by platform rather
than by company means a single scraper reaches every employer hosted
on that platform."""

# JobPilot — illustrative dependency list
#
# This project is not runnable, so these versions are not pinned to
# anything tested. They exist to show what KIND of libraries a real
# implementation would sit on top of: a web framework for the webhook,
# a database client, and an LLM client.

fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
supabase>=2.4
openai>=1.30
python-dotenv>=1.0
